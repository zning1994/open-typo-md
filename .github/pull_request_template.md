<!--
Thanks for the patch. The three headings below are what reviews here actually
turn on — please fill them in rather than deleting them. Anything else you want
to say is welcome underneath.
-->

## What changed

<!-- And why. If it fixes an issue, "Fixes #123". -->

## How it was tested

<!--
`pnpm verify` passing is assumed, not evidence.

For a bug fix: say that you watched the new test fail without the fix. A test
that cannot fail is worse than no test — it occupies the space where a real one
would go.
-->

## Format fidelity

<!--
Required if you touched decoration rules, the Markdown codec, or anything in
packages/markdown. Otherwise write "not affected".

The invariant: open a file, save it, and the bytes are identical. Say what your
change does to that.
-->
